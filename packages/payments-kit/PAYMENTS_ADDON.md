# Payments is an add-on

This base ships a **stub** payments kit — the public types/interface are
here, but the MercadoPago + NOWPayments provider implementations are sold
separately as the **Payments add-on**. Buy it to drop the real impls into
`packages/payments-kit/src/` and set `FEATURE_PAYMENTS=true`.
